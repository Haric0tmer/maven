# Exercice 9

Grâce à Jacoco, je réalise que ma méthode `deleteAccount()` n'a aucun coverage. J'ai donc ajouté un test (`testDeleteAccount()`) pour cette méthode. Après avoir rerun mes tests et regénéré un report Jacoco, je vois bien que la méthode est passée en vert et qu'elle est donc désormais couverte.